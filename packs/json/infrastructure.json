{
  "category": "infrastructure",
  "packs": [
    {
      "id": "infrastructure.terraform",
      "name": "Terraform",
      "description": "Protects against destructive Terraform operations like destroy, taint, and apply with -auto-approve",
      "keywords": [
        "terraform",
        "destroy",
        "taint",
        "state"
      ],
      "safe_patterns": [
        {
          "name": "terraform-plan",
          "regex": "terraform\\s+plan(?!\\s+.*-destroy)"
        },
        {
          "name": "terraform-plan-destroy",
          "regex": "terraform\\s+plan\\s+.*-destroy"
        },
        {
          "name": "terraform-init",
          "regex": "terraform\\s+init"
        },
        {
          "name": "terraform-validate",
          "regex": "terraform\\s+validate"
        },
        {
          "name": "terraform-fmt",
          "regex": "terraform\\s+fmt"
        },
        {
          "name": "terraform-show",
          "regex": "terraform\\s+show"
        },
        {
          "name": "terraform-output",
          "regex": "terraform\\s+output"
        },
        {
          "name": "terraform-state-list",
          "regex": "terraform\\s+state\\s+list"
        },
        {
          "name": "terraform-state-show",
          "regex": "terraform\\s+state\\s+show"
        },
        {
          "name": "terraform-graph",
          "regex": "terraform\\s+graph"
        },
        {
          "name": "terraform-version",
          "regex": "terraform\\s+version"
        },
        {
          "name": "terraform-providers",
          "regex": "terraform\\s+providers"
        }
      ],
      "destructive_patterns": [
        {
          "name": "destroy",
          "regex": "terraform\\s+destroy",
          "reason": "terraform destroy removes ALL managed infrastructure. Use 'terraform plan -destroy' first.",
          "severity": "critical",
          "explanation": "terraform destroy removes ALL managed infrastructure:\n\n- Every resource in your state file is destroyed\n- Cloud resources (VMs, databases, networks) deleted\n- Cannot be undone without backups/recreation\n- Use -target to destroy specific resources only\n\nPreview first: terraform plan -destroy"
        },
        {
          "name": "apply-auto-approve",
          "regex": "terraform\\s+apply\\s+.*-auto-approve",
          "reason": "terraform apply -auto-approve skips confirmation. Remove -auto-approve for safety.",
          "severity": "high",
          "explanation": "terraform apply -auto-approve skips confirmation:\n\n- No opportunity to review changes before applying\n- Intended for CI/CD, not interactive use\n- Changes may destroy or recreate resources\n\nFor safety: remove -auto-approve and review the plan"
        },
        {
          "name": "taint",
          "regex": "terraform\\s+taint\\b",
          "reason": "terraform taint marks a resource to be destroyed and recreated on next apply.",
          "severity": "high",
          "explanation": "terraform taint marks resource for recreation:\n\n- Resource will be destroyed on next apply\n- New resource created with same config\n- May cause downtime during recreation\n- IP addresses and identifiers may change\n\nUse -replace in plan/apply instead (Terraform 0.15.2+)"
        },
        {
          "name": "state-rm",
          "regex": "terraform\\s+state\\s+rm\\b",
          "reason": "terraform state rm removes resource from state without destroying it. Resource becomes unmanaged.",
          "severity": "high",
          "explanation": "terraform state rm orphans resources:\n\n- Resource removed from Terraform state\n- Actual cloud resource still exists\n- Resource becomes 'unmanaged' (Terraform ignores it)\n- May cause drift between state and reality\n\nBack up state first: terraform state pull > backup.tfstate"
        },
        {
          "name": "state-mv",
          "regex": "terraform\\s+state\\s+mv\\b",
          "reason": "terraform state mv moves resources in state. Incorrect moves can cause resource recreation.",
          "severity": "high",
          "explanation": "terraform state mv moves resources in state:\n\n- Renames resource address in state file\n- Wrong move can cause destruction/recreation\n- Use -dry-run to preview the move first\n- Does not affect actual cloud resources\n\nPreview first: terraform state mv -dry-run SOURCE DEST"
        },
        {
          "name": "force-unlock",
          "regex": "terraform\\s+force-unlock\\b",
          "reason": "terraform force-unlock removes state lock. Only use if lock is stale.",
          "severity": "high",
          "explanation": "terraform force-unlock removes state locks:\n\n- Forces removal of a state lock\n- May cause corruption if another process is running\n- Only use when you're sure no other operation is active\n- Lock ID required to prevent accidents\n\nVerify no other operations: check CI/CD pipelines, other users"
        },
        {
          "name": "workspace-delete",
          "regex": "terraform\\s+workspace\\s+delete\\b",
          "reason": "terraform workspace delete removes a workspace. Ensure it's not in use.",
          "severity": "medium",
          "explanation": "terraform workspace delete removes workspace:\n\n- Workspace and its state file deleted\n- Does NOT destroy actual infrastructure\n- Resources become unmanaged (orphaned)\n- Cannot be undone without state backup\n\nDestroy resources first: terraform destroy, then delete workspace"
        }
      ]
    },
    {
      "id": "infrastructure.ansible",
      "name": "Ansible",
      "description": "Protects against destructive Ansible operations like dangerous shell commands and unchecked playbook runs",
      "keywords": [
        "ansible",
        "playbook"
      ],
      "safe_patterns": [
        {
          "name": "ansible-check",
          "regex": "ansible(?:-playbook)?\\s+.*--check"
        },
        {
          "name": "ansible-diff",
          "regex": "ansible(?:-playbook)?\\s+.*--diff"
        },
        {
          "name": "ansible-list-hosts",
          "regex": "ansible(?:-playbook)?\\s+.*--list-hosts"
        },
        {
          "name": "ansible-list-tasks",
          "regex": "ansible(?:-playbook)?\\s+.*--list-tasks"
        },
        {
          "name": "ansible-syntax",
          "regex": "ansible(?:-playbook)?\\s+.*--syntax-check"
        },
        {
          "name": "ansible-inventory",
          "regex": "ansible-inventory"
        },
        {
          "name": "ansible-doc",
          "regex": "ansible-doc"
        },
        {
          "name": "ansible-config",
          "regex": "ansible-config"
        }
      ],
      "destructive_patterns": [
        {
          "name": "shell-rm-rf",
          "regex": "ansible\\s+.*-m\\s+(?:shell|command)\\s+.*rm\\s+-rf",
          "reason": "Ansible shell/command with 'rm -rf' is destructive. Review carefully.",
          "severity": "critical",
          "explanation": "Running 'rm -rf' via Ansible shell or command module executes destructive deletion across all targeted hosts simultaneously. This multiplies the impact compared to running it locally:\n\n- Files deleted on every host in inventory or pattern\n- No confirmation or dry-run by default\n- Parallel execution means rapid, widespread destruction\n- Cannot be undone without backups on each host\n\nSafer alternatives:\n- Use file module with state=absent for managed deletion\n- Add --check flag to preview which hosts would be affected\n- Use --limit to restrict to specific hosts first\n- Verify target path with debug module before deletion"
        },
        {
          "name": "shell-reboot",
          "regex": "ansible\\s+.*-m\\s+(?:shell|command)\\s+.*(?:reboot|shutdown|poweroff)",
          "reason": "Ansible shell/command with reboot/shutdown affects system availability.",
          "severity": "high",
          "explanation": "Rebooting or shutting down systems via Ansible affects all targeted hosts at once. This can cause service outages across your infrastructure:\n\n- All services on affected hosts become unavailable\n- No graceful draining of connections or traffic\n- Database transactions may be interrupted\n- Load balancer health checks will fail\n\nSafer alternatives:\n- Use the reboot module which handles wait_for_connection\n- Add serial: 1 to playbook for rolling reboots\n- Use --limit to test on single host first\n- Coordinate with maintenance windows"
        },
        {
          "name": "extra-vars-delete",
          "regex": "ansible\\s+.*-e\\s+['\"].*(?:delete|remove|destroy|drop)",
          "reason": "Ansible extra-vars contains potentially destructive keywords. Review carefully.",
          "severity": "medium",
          "explanation": "Extra variables containing destructive keywords like 'delete', 'remove', 'destroy', or 'drop' may trigger conditional destructive actions in playbooks:\n\n- Variables may control whether resources are deleted\n- Typos in variable values could trigger unintended paths\n- Variable precedence may override safer defaults\n\nSafer alternatives:\n- Review playbook to understand how variables are used\n- Add --check to see what tasks would run\n- Use --limit to test on single host first\n- Consider using vault-encrypted vars for destructive flags"
        }
      ]
    },
    {
      "id": "infrastructure.pulumi",
      "name": "Pulumi",
      "description": "Protects against destructive Pulumi operations like destroy and up with -y (auto-approve)",
      "keywords": [
        "pulumi",
        "destroy",
        "state"
      ],
      "safe_patterns": [
        {
          "name": "pulumi-preview",
          "regex": "pulumi\\s+preview"
        },
        {
          "name": "pulumi-stack-ls",
          "regex": "pulumi\\s+stack\\s+ls"
        },
        {
          "name": "pulumi-stack-select",
          "regex": "pulumi\\s+stack\\s+select"
        },
        {
          "name": "pulumi-stack-init",
          "regex": "pulumi\\s+stack\\s+init"
        },
        {
          "name": "pulumi-config",
          "regex": "pulumi\\s+config"
        },
        {
          "name": "pulumi-whoami",
          "regex": "pulumi\\s+whoami"
        },
        {
          "name": "pulumi-version",
          "regex": "pulumi\\s+version"
        },
        {
          "name": "pulumi-about",
          "regex": "pulumi\\s+about"
        },
        {
          "name": "pulumi-logs",
          "regex": "pulumi\\s+logs"
        }
      ],
      "destructive_patterns": [
        {
          "name": "destroy",
          "regex": "pulumi\\s+destroy",
          "reason": "pulumi destroy removes ALL managed infrastructure. Use 'pulumi preview --diff' first.",
          "severity": "critical",
          "explanation": "pulumi destroy removes ALL managed infrastructure:\n\n- Every resource in your stack is destroyed\n- Cloud resources (VMs, databases, networks) deleted\n- Cannot be undone without backups/recreation\n- Use --target to destroy specific resources only\n\nPreview first: pulumi preview --diff"
        },
        {
          "name": "up-yes",
          "regex": "pulumi\\s+up\\s+.*(?:-y\\b|--yes\\b)",
          "reason": "pulumi up -y skips confirmation. Remove -y flag for safety.",
          "severity": "high",
          "explanation": "pulumi up -y skips confirmation:\n\n- No opportunity to review changes before applying\n- Intended for CI/CD, not interactive use\n- Changes may destroy or recreate resources\n- Replacements can cause downtime\n\nFor safety: remove -y and review the preview"
        },
        {
          "name": "state-delete",
          "regex": "pulumi\\s+state\\s+delete",
          "reason": "pulumi state delete removes resource from state without destroying it.",
          "severity": "high",
          "explanation": "pulumi state delete orphans resources:\n\n- Resource removed from Pulumi state\n- Actual cloud resource still exists\n- Resource becomes 'unmanaged' (Pulumi ignores it)\n- May cause drift between state and reality\n\nConsider: pulumi refresh to sync state with reality"
        },
        {
          "name": "stack-rm",
          "regex": "pulumi\\s+stack\\s+rm",
          "reason": "pulumi stack rm removes the stack. Use --force only if stack is empty.",
          "severity": "high",
          "explanation": "pulumi stack rm removes the entire stack:\n\n- Stack and its state deleted\n- Does NOT destroy actual infrastructure (unless empty)\n- --force required if resources still exist\n- Resources become unmanaged (orphaned)\n\nDestroy resources first: pulumi destroy, then rm stack"
        },
        {
          "name": "refresh-yes",
          "regex": "pulumi\\s+refresh\\s+.*(?:-y\\b|--yes\\b)",
          "reason": "pulumi refresh -y auto-approves state changes. Review changes first.",
          "severity": "medium",
          "explanation": "pulumi refresh -y auto-approves state sync:\n\n- Syncs Pulumi state with actual cloud resources\n- May delete resources from state if not found\n- May update state with drift from cloud\n\nRun without -y first to review detected changes"
        },
        {
          "name": "cancel",
          "regex": "pulumi\\s+cancel",
          "reason": "pulumi cancel terminates an in-progress update, which may leave resources in inconsistent state.",
          "severity": "high",
          "explanation": "pulumi cancel stops in-progress operations:\n\n- Terminates currently running update/destroy\n- Resources may be left in inconsistent state\n- Some resources created, others not\n- May require manual cleanup\n\nUse only when operation is stuck/hung"
        }
      ]
    },
    {
      "id": "infrastructure.cdk",
      "name": "AWS CDK",
      "description": "Protects against destructive AWS CDK operations like cdk destroy",
      "keywords": [
        "cdk",
        "destroy"
      ],
      "safe_patterns": [
        {
          "name": "cdk-list",
          "regex": "(?i)cdk\\\\s+ls"
        },
        {
          "name": "cdk-diff",
          "regex": "(?i)cdk\\\\s+diff"
        },
        {
          "name": "cdk-synth",
          "regex": "(?i)cdk\\\\s+synth"
        }
      ],
      "destructive_patterns": [
        {
          "name": "cdk-destroy",
          "regex": "(?i)cdk\\\\s+destroy",
          "reason": "cdk destroy permanently removes the specified stack and its resources.",
          "severity": "critical",
          "explanation": "Destroying a CDK stack removes all resources defined in that stack from your AWS account. This can include databases, compute instances, and networking infrastructure. Data in resources without a retention policy will be permanently lost.\n\nPreview changes first:\n  cdk diff\n\nList stacks:\n  cdk ls"
        }
      ]
    },
    {
      "id": "infrastructure.serverless",
      "name": "Serverless Framework",
      "description": "Protects against destructive Serverless operations like sls remove",
      "keywords": [
        "sls",
        "serverless",
        "remove"
      ],
      "safe_patterns": [
        {
          "name": "sls-info",
          "regex": "(?i)(?:sls|serverless)\\s+info"
        },
        {
          "name": "sls-package",
          "regex": "(?i)(?:sls|serverless)\\s+package"
        }
      ],
      "destructive_patterns": [
        {
          "name": "sls-remove",
          "regex": "(?i)(?:sls|serverless)\\s+remove",
          "reason": "sls remove permanently deletes the deployed service and its resources.",
          "severity": "critical",
          "explanation": "Removing a Serverless service deletes the CloudFormation stack and all associated resources (Lambda, API Gateway, etc.) in your provider. This is irreversible and will stop all service functionality immediately.\n\nCheck service info first:\n  sls info"
        }
      ]
    }
  ]
}
