version: 1

env:
  - CGO_ENABLED=0

binary: tw
dir: ./cmd/tw

goos:
  - linux
  - darwin
  - windows

goarch:
  - amd64
  - arm64

flags:
  - -trimpath

ldflags:
  - "-s -w"
  - "-X github.com/thgrace/training-wheels/internal/cli.Version={{ .Env.VERSION }}"