version: 1

env:
  - CGO_ENABLED=0

binary: tw-darwin-amd64
dir: ./cmd/tw

goos: darwin
goarch: amd64

flags:
  - -trimpath

ldflags:
  - "-s -w"
  - "-X github.com/thgrace/training-wheels/internal/cli.Version={{ .Env.VERSION }}"
