version: 1

env:
  - CGO_ENABLED=0

binary: tw-windows-amd64.exe
dir: ./cmd/tw

goos: windows
goarch: amd64

flags:
  - -trimpath

ldflags:
  - "-s -w"
  - "-X github.com/thgrace/training-wheels/internal/cli.Version={{ .Env.VERSION }}"
